<div class= "container">
<h1>Insight page</h1>
<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
<br>
<%= link_to "quizz", edit_user_path(current_user) %>
<%= Search.last.latitude %>
<%= Search.last.longitude %>
</div>
